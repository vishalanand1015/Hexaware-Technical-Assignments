import { CardhowerDirective } from './cardhower.directive';

describe('CardhowerDirective', () => {
  it('should create an instance', () => {
    const directive = new CardhowerDirective();
    expect(directive).toBeTruthy();
  });
});
